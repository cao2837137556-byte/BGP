# 实验主线总表

最后更新：2026-05-22
定位：项目唯一长期维护的实验主线表。
维护方式：按时间推进顺序维护一张表；后续新实验直接在末尾追加新行，并同步更新结论与状态。

| 时间顺序 | 实验编号 | 核心问题 | 固定口径 / 变量 | 关键结果摘要 | 当前结论 / 标签 | 状态 | 主要产物 / 代码 |
|---|---|---|---|---|---|---|---|
| 01 | E1-E3 | 主链基础收敛、高优质量、uncertain 增益 | 基于基线 run `20260313T032554_4f9be28c` | 形成主表、漏斗表、高优质量表、uncertain 增益表 | 基础盘固定，可作为写作起点 | 已完成 | `论文/实验资产_E1E2E3_v01/` |
| 02 | E4 | 多窗口稳定性 | 多 run 对比，主链不改 | forged-origin 分层链不依赖单一幸运 run | 稳定性可用于方法可靠性支撑 | 已完成 | `论文/实验执行_E4_稳定性_v01/` |
| 03 | E5 | 参数敏感性 | 默认参数附近扰动 | 参数整体稳定，但 `min_weak_rules` 解释后续以 E6-A 为准 | E5 保留，candidate 解释由 E6-A 接管 | 已完成 | `论文/实验执行_E5_参数敏感性_v01/` |
| 04 | E6-A | candidate 是否真实有效 | 只审计 `min_weak_rules` 口径，不改后层 | candidate `1680/1106/1098/1098`，final high 恒定 `24` | `candidate=真实有效`，主要负责前置压缩 | 已完成 | `论文/实验执行_E6A_candidate审计_v01/` |
| 05 | E6-B-1 | gate 是否独立有效 | default vs no-gate | high `24 -> 265`，新增 high `241`，质量明显退化 | `gate=独立有效` | 已完成 | `outputs/e6b1_nogate_ablation_v02/` |
| 06 | E6-B-2 | augment 是否独立有效 | default vs no-augment | high `24 -> 22`，2 条 high 回落，18 条 low 回流 needs | `augment=作用有限` | 已完成 | `outputs/e6b2_noaugment_ablation_v01/` |
| 07 | E6-B-3 | gate 与 augment 是分工还是冗余 | no-gate + no-augment 联合消融 | high `266`，比 no-gate 仅 `+1` | `joint_ablation=支持分层分工` | 已完成 | `outputs/e6b3_joint_ablation_v01/` |
| 08 | E7-A | 可见度下降时系统如何退化 | full vs single-collector | full high `24`，sg_only `4`，rrc00_only `1`，无 new high 误抬升 | `visibility_ablation=支持主线` | 已完成 | `outputs/e7a_visibility_ablation_v01/` |
| 09 | E7-B | 单 collector 是否存在视角结构差异 | `route-views.sg_only` vs `rrc00_only` | candidate 交集 `33`，needs 交集 `33`，high 交集 `0` | `collector_structure=存在显著差异` | 已完成 | `outputs/e7b_collector_structure_v01/` |
| 10 | E8-A | 哪些事件最适合作为论文案例 | 从 E6/E7 已有结果选样 | 正式池 `9` 条，D 类候补 `3` 条，共 `12` 条 | `case_pool=已可进入正式E8` | 已完成 | `outputs/e8a_case_pool_v01/` |
| 11 | E8-B | 高价值事件能否做外部验证挂载 | 对 12 条案例做控制面 / 外部证据补充 | `strongly_suspicious=8`，`needs_external_review=4` | `external_validation=形成初步validated suspicious set` | 已完成 | `outputs/e8b_case_external_validation_v01/` |
| 12 | E8-C | 少量公开已知事件能否背书系统能力 | 候选 4，入选 3 | 命中 `1/3`，Rostelecom 命中 high | 小规模支持，但 visibility 限制明显 | 已完成 | `outputs/e8c_known_events_test_v01/` |
| 13 | E9-A | 扩大 collector 后，已知事件命中是否改善 | baseline `2 collector` vs expanded `8 collector` | `1/3 -> 3/3 response`，其中 `2 high + 1 needs` | `known_events_visibility=强支持` | 已完成 | `outputs/e9a_known_event_visibility_v02/` |
| 14 | E9-B | Google-Verizon 为什么在 review 而不是 high | route-leak 专项审计 | target prefix 进入 `needs_review=2` | route-leak 风格事件稳定 review 不是失败 | 已完成 | `outputs/e9b_route_leak_review_audit_v01/` |
| 15 | E9-C | 如何把已知事件测试做成可扩容库存 | manifest + inventory | manifest `7` 个事件，core / next_batch / backup 三层开始成形 | 已知事件不再依赖脚本常量 | 已完成 | `data/known_events/known_event_candidates_v02+.json`，`scripts/build_e9c_known_event_inventory.py` |
| 16 | E9-D | MainOne 为什么 exact-prefix miss | exact-prefix vs ordered leak-chain 审计 | exact-prefix `0`，chain raw `1193 -> 4213`，final mostly needs | route-leak 不能只用单前缀判卷 | 已完成 | `outputs/e9d_route_leak_gap_audit_v01/` |
| 17 | S0-A | 是否需要双轨制阅卷标准 | forged-origin 看 high，route-leak 看 needs/high | `MainOne` 从 miss 变为 `Hit Expected` | 双轨制评估成立 | 已完成 | `outputs/s0_dual_track_eval_v01/` |
| 18 | E9-E | Google-Verizon 是否应迁到 chain-anchor | `286 -> 701 -> 15169 -> 4713` 审计 | expanded chain final Google-origin prefixes `9407`，主要落在 needs | Google-Verizon 应转入 route-leak 口径 | 已完成 | `outputs/e9e_google_verizon_chain_audit_v01/` |
| 19 | S0-B | Google-Verizon 迁入双轨后结论是否稳定 | dual-track v02 | hit_expected `6 -> 7`，Google expanded=`Hit Expected` | core 事件形成 `forged-origin/high` 与 `route-leak/needs` 分工 | 已完成 | `outputs/s0_dual_track_eval_v02/` |
| 20 | E9-F | inventory 是否已与双轨制对齐 | `manifest_v04 + dual-track_v02` | baseline hit `2`，expanded hit `5` | inventory 对齐双轨口径 | 已完成 | `outputs/e9c_known_event_inventory_v03/` |
| 21 | E9-G | backup 池首轮复测结果如何 | 事件：Indosat + Pakistan | `Indosat` expanded=`needs_review`，`Pakistan` 原窄窗=`no_visibility` | `Indosat` 属于 route-leak 正确隔离命中 | 已完成 | `outputs/e9f_backup_visibility_v01/` |
| 22 | E9-H | inventory 是否已纳入 backup 池 | 全部 7 个已知事件统一汇总 | expanded 主库存达到 `3 high + 3 needs + 1 no_visibility` | 已知事件库存可直接复用 | 已完成 | `outputs/e9c_known_event_inventory_v04/` |
| 23 | E9-I | Pakistan 是否真的完全不可见 | 原窗口前后各扩 2 小时审计 | baseline 可见 `35` 行，expanded 可见 `201` 行 | 原始 miss 主要来自窄时间窗，不再继续死磕 | 已完成 | `outputs/e9i_pakistan_time_window_audit_v01/` |
| 24 | S1-A | 2024 现代数据面 pilot 能否稳定跑通 | modern 2024，baseline `2 collector`，expanded `12 collector` | baseline：`events=614892`，`candidate=276652`，`high=20284`；expanded：`events=2628659`，`candidate=1610525`，`high=138525`，`needs=732603`，`low=739397`；旧 gate / final 在 modern 规模下失稳，已完成流式化修复 | modern 2024 60 分钟 pilot 已跑通，现代阶段可继续放大时间窗与 collector 覆盖 | 已完成 | `outputs/s1a_modern_2024_pilot_60m_v02/`，`scripts/run_s1a_modern_2024.py`，`scripts/build_weak_candidates_streaming.py`，`scripts/score_weak_candidates_streaming.py`，`scripts/gate_scored_candidates_streaming.py`，`scripts/build_final_alerts_streaming.py` |
| 25 | S1-B | expanded high 为什么膨胀，哪些 high 最值得降回 needs_review | 只审计 S1-A 现有产物，不重跑主链；strict key=`prefix+origin_as+as_path_clean`，relaxed key=`prefix+origin_as` | expanded final high=`138525`；`clean=0`，`fragile=101279`，`noisy=37246`；其中 noisy-high `37233` 来自 `augmentation_promoted` 且 `missing=true`；strict expanded-only high keys=`84188`，relaxed expanded-only high keys=`26411` | `modern_purity_audit=已定位主要膨胀来源`；当前最该收紧的是 augment / score，不是 gate | 已完成 | `outputs/s1b_modern_high_audit_v01/`，`scripts/run_s1b_purity_audit.py` |
| 26 | S1-C | 只收紧 augment，能否止住 modern noisy-high 膨胀 | 固定 S1-B 质量桶；只改 augment promotion；R0 vs A1/A2/A3；Join Key=`event_id` | R0：`high=138525`，`missing_rate=0.2688`，`noisy=37246`；A1：`high=101292`，`missing_rate=0`，`noisy=13`；A2 与 A1 完全一致；A3：`high=88916`，`noisy=13`，但额外压掉更多 augment-high | `augment_tightening=已找到有效止血方案`；最优小步变体为 `A1_missing_block`，A2 无额外收益，A3 偏激进 | 已完成 | `outputs/s1c_augment_tightening_v01/`，`scripts/run_s1c_augment_tightening.py` |
| 27 | S1-D | A1_missing_block 并回 modern 主流程后，是否能稳定复现新稳态 | 只重走 `augment+final`；modern profile=`modern_missing_block`；最小历史兼容性 spot-check 使用 `e9a_expanded_v01_rostelecom_20170426` | S1-D end-to-end：`high=101292`，`needs=769836`，`low=739397`，与 S1-C A1 严格一致；`high_missing_rate=0`，`noisy=13`，`gating_likely_malicious_to_high=85511` 不变；历史 spot-check 有 `31` 条 `augmentation_promoted + missing=true` 从 high 回落到 needs，但 `gate_high_retained_ratio=1.0` | `s1d_modern_baseline=基本成功但需复查`；modern 新稳态已确立，但历史全局默认行为仍不宜直接切换 | 已完成 | `outputs/s1d_modern_new_baseline_v01/`，`scripts/run_s1d_modern_new_baseline.py`，`scripts/augment_uncertain_candidates.py` |
| 28 | S1-E | augment 为什么 60 分钟 modern 数据要跑约 110 分钟 | 不改业务逻辑；对 `modern_missing_block` 做确定性小样本 profiling；样本 `20000` 条 uncertain | 总耗时 `207.88s`；`core_augment_loop=198.04s`（`95.27%`）；`calc_multi_view_support_score=151.08s`（核心 loop `76.29%`）；`near_time=124.35s`（核心 loop `62.79%`）；I/O 仅 `6.78s` | `s1e_profiling=瓶颈已定位`；瓶颈是逐行 DataFrame 过滤 / take / Series 构造，不是 I/O，也不是 merge/groupby | 已完成 | `outputs/s1e_augment_profiling_v01/`，`scripts/run_s1e_augment_profiling.py` |
| 29 | S1-F | 能否只优化 augment 实现而不改变业务判定 | 保守方案：新增 fast augment 引擎，不替换原脚本；全量对齐 S1-D；只优化 `calc_multi_view_support_score` 的近时间窗查找与 collector 支持计算 | full modern 60m：augment `6634.60s -> 399.29s`，提速 `16.62x`；final `42.02s`；augment 输出与 S1-D `878063/878063` 行严格一致，label / evidence / subscore / blocked flags mismatch 全为 `0`；final 输出 `1610525/1610525` 行严格一致，final label mismatch `0` | `s1f_optimization=提速成功_业务无损`；性能瓶颈已显著缓解，可恢复 modern 扩窗复测，但仍需监控全链路其他阶段耗时 | 已完成 | `outputs/s1f_augment_optimization_v01/`，`scripts/augment_uncertain_candidates_fast.py`，`scripts/run_s1f_augment_optimization.py` |
| 30 | S2-A | 2024 modern 窗口从 60 分钟扩到 6 小时后是否还能本地稳定复测 | 不改算法；新增 `pilot_6h_april16`；使用 `modern_missing_block + fast augment`；先跑 baseline 2 collectors，再评估是否继续 expanded 12 collectors | baseline 6h 完成：`events=3431103`，`candidate=1804382`，`high=123849`，`needs=605585`，`low=1074948`，`high_missing_rate=0`；Docker 容器在 baseline augment 被 `-9` 杀掉，但 host fast augment 成功 `354.6s`，final `54.8s`；12-collector expanded 未继续本地硬跑 | `s2a_modern_6h=baseline_completed_expanded_deferred_by_resource_stop`；S1-F 解决了 augment 计算瓶颈，但 6h 本地串行 collection/orchestration 成为新瓶颈 | 暂停/待续 | `outputs/s2a_modern_2024_6h_v01/`，`data/runs/s2a_baseline_v01_pilot_6h_april16/` |
| 31 | S2-A-HPC-Prep | 如何让 expanded 12 collectors x 6h 在超算上可断点、可补采、可继续实验 | 不改检测主链；新增 HPC 编排脚本：collector-wise array 采集、collector run 合并、downstream 单独执行；允许失败 collector 留档后补采 | 新增 `run_s2a_collect_one.py` 支持单 collector 安全 resume；新增 `run_s2a_merge_collector_runs.py` 合并 collector run；新增 `run_s2a_downstream_from_raw.py` 从 merged raw 跑 label/event/baseline/candidate/score/gate/fast-augment/final；新增两个 slurm 模板 | `s2a_hpc_pipeline=已准备可执行`；先做了超算直拉尝试，但结果不稳定 | 已完成 | `scripts/run_s2a_collect_one.py`，`scripts/run_s2a_merge_collector_runs.py`，`scripts/run_s2a_downstream_from_raw.py`，`scripts/hpc/s2a_collect_array.slurm`，`scripts/hpc/s2a_merge_downstream.slurm` |
| 32 | S2-A-LocalRaw | 现代 6h expanded raw 数据是否应改由本地稳定拉取，再交给超算做计算 | 明确分层：本地/Docker 负责 raw parquet 拉取；超算只负责 merge/downstream；不再让超算直接在线拉 BGPStream | 首轮超算直拉 6h array 中，多 collector 长时间卡在 `scripts/run.py`，`live.log` 无新增且 parquet 仅少量落盘，确认问题主要在远端数据拉取稳定性，不在主链计算；新增 `run_s2a_local_collect.py` 固化本地 raw 采集入口 | `s2a_collection_strategy=local_raw_hpc_downstream`；后续 modern 大窗口默认按此路线执行 | 进行中 | `scripts/run_s2a_local_collect.py`，`outputs/s2a_local_collect_v01/`，`scripts/hpc/s2a_merge_downstream.slurm` |
| 33 | S2-B1 | score 阶段是否已成为 6h expanded 新瓶颈，能否在不改业务语义下优化 | 不重跑 raw/events/baseline/candidate；保留原 scorer 作 reference；新增 fast scorer + part checkpoint；本地用 60m S1-A 资产做 correctness + benchmark | S2-A 6h 已完成资产：`events=15667871`、`candidate=10236431`、`candidate_rate=65.33%`；S2-B1 correctness sample `50000` rows，实际 scored `31556`，关键字段 mismatch `0`；fast benchmark `27162.13 rows/sec`，相对 reference sample `6.75x`，估算 full `10.236M` score 约 `376.86s` | `s2b_score_optimization=本地验证通过_待超算续跑`；score 可从瓶颈转为可 checkpoint 续跑阶段 | 已完成 | `scripts/score_weak_candidates_streaming_fast.py`，`scripts/run_s2b_score_validation.py`，`scripts/run_s2b_score_resume.py`，`scripts/hpc/s2b_score_resume.slurm`，`outputs/s2b_score_optimization_v01/` |
| 34 | S2-B2 | 6h expanded 是否能不重跑前置阶段，直接从 score 续跑到 final | 固定 run `s2a_expanded_v01_pilot_6h_april16`；只跑 fast score -> gate -> fast augment -> final -> bundle；不重跑 raw/events/baseline/candidate | score stage `169.87s`，scorer core `123.20s`、`83089.77 rows/sec`、`63` parts；gate `631.36s`，fast augment `2055.74s`，final `1627.28s`；`events=15667871`，`candidate=scored=gated=10236431`，`high=639548`，`needs=4839755`，`low=4757128`，`high_missing_rate=0.0`，`gating_high=581112`，`augment_high=58436` | `s2b_score_resume=完成`；score 瓶颈解除，expanded 6h 全链路已稳定跑到 final；下一步应做 6h high composition / purity audit，而不是直接跳 24h | 已完成 | `outputs/s2b_score_optimization_v01/`，`outputs/bundles/s2b_score_optimization_bundle.zip`，`data/runs/s2a_expanded_v01_pilot_6h_april16/` |
| 35 | S2-C | 6h expanded high 池是否仍保持可解释、可控，没有出现新的 noisy-high 模式 | 只读 S2-B2 产物；审计 high quality bucket、source layer、hourly stability、augment promoted high、top prefix-origin、与 S1-D/S1-F 60m 参考按小时对比；不重跑检测主链 | `events=15667871`，`candidate=10236431`，`high=639548`，`needs=4839755`，`low=4757128`，`high_missing_rate=0.0`；`clean_high=0`，`fragile_high=639535`，`noisy_high=13`；high 来源：gate `581112`（`90.86%`），augment `58436`（`9.14%`）；hourly high rate `5.72%~6.67%`，无小时尖峰；相对 S1-D/S1-F 60m 按小时：candidate `1.059x`、high `1.052x`、needs `1.048x`、low `1.072x`；noisy-high 13 条均来自 gate high conflict，不来自 augment promotion | `s2c_high_audit=通过`；6h expanded 没有复发 missing=true noisy-high 膨胀，也未发现新的 augment noisy-high 模式；可进入 S2-D 24h 扩窗资源规划与执行准备 | 已完成 | `outputs/s2c_expanded_6h_high_audit_v01/`，`outputs/bundles/s2c_expanded_6h_high_audit_bundle.zip`，`scripts/run_s2c_high_audit.py`，`scripts/hpc/s2c_high_audit.slurm` |
| 36 | S3-A | 如何把 6h expanded 的 high/needs event-level 输出变成可审 incident tickets | 不改上游检测主链；只消费 `final_alerts.parquet` 与 `event_units.parquet`；首版 conservative two-level aggregation：micro `15min`，macro `2h`；family=`forged_origin_like/route_leak_like/stealth_visibility_like/unknown_weak_signal` | `raw_alert_count=5479303`，其中 high `639548`、needs `4839755`；micro incidents `1839957`；incident tickets `217165`；compression_ratio `25.231059`，analyst_workload_reduction `0.960366`；family tickets：forged_origin_like `216801`、route_leak_like `364`；priority tickets：P1 `42047`、P2 `13039`、P3 `162079`；3 个 `dominant_origin_as=NA` 超大 P2 incident 覆盖 `348708` 条 needs | `s3a_incident_aggregation=完成但需校准`；incident layer 有效完成 event->ticket 评估单位转换，route-leak-like 与 forged-origin-like 已分队列；但 P1/P2 规模仍大，且 NA-origin 背景工单被推入 P2，进入 S3-B 前需要 S3-A2 priority/NA-origin 校准 | 已完成 | `outputs/s3a_incident_aggregation_v01/`，`data/runs/s2a_expanded_v01_pilot_6h_april16/incidents/`，`outputs/bundles/s3a_incident_aggregation_bundle.zip` |
| 37 | S3-A2 | S3-A tickets 如何校准优先级并处理 unknown-origin 超大背景工单 | 不重跑上游检测链；只读 S3-A `incident_tickets.parquet` / `incident_membership.parquet`；保留原始 `incident_priority`，新增 `calibrated_priority/calibration_flags/calibration_reason` | membership 校验 `5479303` rows、`217165` incidents、member_count mismatch `0`；校准前 tickets：P1 `42047`、P2 `13039`、P3 `162079`；校准后 tickets：P1 `41885`、P2 `13198`、P3 `162082`；校准前 member coverage：P1 `2775776`、P2 `1999368`、P3 `704159`；校准后：P1 `2565156`、P2 `1861280`、P3 `1052867`；降级 `165` tickets / `559328` members，其中 P1->P2 `162`，P2->P3 `3`；3 个 NA-origin 超大 P2 全部降为 P3，覆盖 `348708` needs、`0` high | `s3a2_priority_calibration=完成`；明显坏工单已隔离，calibrated P1/P2 更适合 S3-B 审计；但 P1/P2 仍大，说明后续重点仍是 noise source audit 与检测能力升级，不是继续调阈值 | 已完成 | `scripts/run_s3a2_incident_priority_calibration.py`，`project_docs/S3A2_PRIORITY_CALIBRATION.md`，`outputs/s3a2_incident_priority_calibration_v01/` |
| 38 | S3-B | high/needs 和 P1/P2 到底由哪些噪声结构支撑 | 不改检测主链；只读 S3-A/S3-A2 incident outputs 与 membership；按 priority/family/reason/origin/prefix/path/fan-out 分解 suspicious rows 和 incident tickets | calibrated P1/P2 `55083` tickets / `4426436` rows，其中 high `632640`、needs `3793796`；large fan-out `1832` tickets，其中 P1/P2 `1829` tickets / `1102322` rows；needs 来源：with-high weak support `2826078`、single-collector/sparse/short-lived `1242749`、large-fanout background-like `764874`、route-leak-like `3952`、forged-origin residual `2102`；最大 P1/P2 pattern 为 `single_collector_visibility+structural_novelty_score+unseen_path_for_prefix_origin+unusually_short_duration_for_prefix`，覆盖 `27120` tickets / `3943362` rows，weighted high share `0.095` | `s3b_noise_audit=完成`；P1/P2 偏大主因是低可见度/短时 path novelty 在 clean stable window 中过度贡献；large fan-out 是重要噪声源但不是唯一噪声源；route-leak-like 与 forged-origin-like 应继续分队列；下一步进入 S3-C detection capability upgrade design | 已完成 | `scripts/run_s3b_noise_source_audit.py`，`project_docs/S3B_NOISE_SOURCE_AUDIT.md`，`outputs/s3b_noise_source_audit_v01/` |
| 39 | S3-C | 如何把 S3-A/S3-B 暴露的问题转为检测能力升级设计 | 不先上深度学习；先做白盒语义增强设计，候选方向包括 role churn、AS Hegemony delta、forged-origin path plausibility、route-leak triplet legality、RPKI/IRR/PeeringDB、NO_EXPORT/communities | 待执行 | 目标：把当前主矛盾从“能跑通/能聚合”推进到“能把值得看的弱信号排到前面”；明确哪些增强进入 score/gate/augment/verification | 待执行 | `project_docs/S3_DETECTION_QUALITY_ROADMAP.md` |
| 40 | S3-C1 | 低可见度/短时 unseen-path novelty 是否可以通过白盒 path plausibility 离线降权 | 不重跑上游；不覆盖 score/gate/final/incidents；只新增 `adjusted_risk_score_s3c1`、`adjusted_risk_bucket_s3c1`；pattern_A 默认降权，pattern_B 只诊断 | S2 fixed-run full 完成：input/loaded/joined `10236431/10236431/10236431`，missing join `0`；pattern_A `7440623` rows，adjusted down `6885990`；pattern_B `659862` rows，adjusted down `0`；risk bucket changed `4576319` rows；score high `2047659 -> 1187117`，medium `4094284 -> 937913`，low `4094488 -> 8111401`；P1/P2 joined `4426436` rows，其中 adjusted down `3510753`，touched P1/P2 incidents `32469` | `s3c1_full_completed_effective_but_too_strong`；S3-C1 成功识别 dominant pattern 且保护 pattern_B，但 default penalty 造成 bucket 迁移过大，不能直接并入主链；下一步先做 S3-C1b 权重/penalty 校准，并补 known-event regression | 已完成 | `scripts/run_s3c1_visibility_path_plausibility_pilot.py`，`scripts/hpc/s3c1_visibility_path_plausibility.slurm`，`project_docs/S3C1_VISIBILITY_PATH_PLAUSIBILITY.md`，`outputs/s3c1_visibility_path_plausibility_v01/` |
| 41 | S3-C1b | S3-C1 penalty 如何调到既约束 pattern_A 又不过度迁移 risk bucket | 不重跑上游；不覆盖 score/gate/final/incidents；比较 default、low-only、soft medium、gate-only、medium-gate-only 等离线策略 | fixed S2 full 完成：input/loaded `10236431/10236431`，status `completed`，warnings `0`；推荐仍为 `strategy_medium_gate_only`；default bucket changed `4576319`、high `2047659 -> 1187117`，过强；recommended bucket changed `5183`、high `2047659 -> 2042688`，pattern_A score down `15294`、gate evidence `6870696`，P1/P2 score down `31`、gate evidence `3510722`，touched P1/P2 incidents `32469`；pattern_B adjusted/bucket changed 均 `0`；known-event inventory 可读但 matched rows `0` | `s3c1b_full_completed_recommend_medium_gate_only`；结论支持把 plausibility 作为 S3-C2 gate evidence，而不是直接采用 S3-C1 default penalty；known-event regression 不能因 0 match 过度解读 | 已完成 | `scripts/run_s3c1b_penalty_calibration.py`，`scripts/hpc/s3c1b_penalty_calibration.slurm`，`project_docs/S3C1B_PENALTY_CALIBRATION.md`，`outputs/s3c1b_penalty_calibration_v01/` |
| 42 | S3-C2 | plausibility 是否能作为 gate evidence 而不是直接改 score | 不重跑上游；不覆盖 score/gate/final/incidents；只做离线 gate evidence ablation；所有 label/priority 变化均为 simulated；P1->P2 不算 workload reduction | fixed S2 full 完成：input/loaded `10236431/10236431`，status `completed`，warnings `0`；推荐 `variant_medium_gate_only`；final labels 不变：high `639548 -> 639548`、needs `4839755 -> 4839755`、low `4757128 -> 4757128`；`gate_evidence_rows=6885990`，`pattern_A_gate_evidence_rows=6885990`，`pattern_A_control_rate=0.9255`；pattern_B label changed `0` 且 recommended variant 下 `pattern_B_gate_evidence_rows=0`；P1/P2 burden 不变：tickets `55083 -> 55083`，members `4426436 -> 4426436`，P1->P2 transfer `0`；affected P1/P2 rows `3510753`，touched incidents `32469`；known-event inventory 可读但 matched rows `0` | `s3c2_fixed_s2_completed_evidence_layer_only`；S3-C2 证明 gate evidence 通道可用且保护 pattern_B，但它本身不降低 final/P1/P2 负担；下一步应把 gate evidence 用于 S3-D incident-level verification queue，或并行做 S3-C3 route-leak triplet legality | 已完成 | `scripts/run_s3c2_gate_evidence_ablation.py`，`scripts/hpc/s3c2_gate_evidence_ablation.slurm`，`project_docs/S3C2_GATE_EVIDENCE_ABLATION.md`，`outputs/s3c2_gate_evidence_ablation_v01/` |
| 43 | S3-D | 如何把 S3-C2 gate evidence 转成 incident-level verification queue | 不重跑上游；不覆盖 score/gate/final/incidents；不做外部验证；不把 queue 当真实标签；`weak_label_candidate` 只作未来学习层候选字段 | fixed S2 full 完成：total incidents `217165`，calibrated P1/P2/P3 `41885/13198/162082`；queue 分布：low_priority_background `157351`，patternB_path_abnormal_verification `49852`，gate_evidence_weak_case `7192`，high_confidence_candidate `1216`，background_like_review `1190`，route_leak_like_review `364`；P1/P2 中 gate_evidence_weak_case `7192`，high_confidence_candidate `1216`；pattern_A touched incidents `174046`，pattern_B verification incidents `49852`；S3-D 使用 incident_membership reason_signature 计算 incident-member evidence，非 full S3-C2 event-level evidence | `s3d_verification_queue_schema=完成`；S3-D 成功形成 verification queue/schema，但不改变 final/P1/P2，不声称验证完成；下一步进入 S3-D2 external evidence attachment 或 S3-C3 route-leak triplet legality | 已完成 | `scripts/run_s3d_verification_queue_schema.py`，`project_docs/S3D_VERIFICATION_QUEUE_SCHEMA.md`，`project_docs/REALTIME_UPGRADE_ROADMAP.md`，`outputs/s3d_verification_queue_schema_v01/` |
| 44 | S3-D2 | verification queue 是否能挂载第一批外部/来源证据字段 | 不重跑上游；不覆盖主链；不做最终真假判定；RPKI/AS-rel/known-event 均带 provenance；没有历史 cache 不做强证据 | fixed S2 full 完成：total incidents `217165`；evidence buckets：weak `201329`，medium `15836`，strong `0`；RPKI `unavailable=217165`，因为缺 2024 历史 RPKI cache；AS-rel `weak_stale_snapshot=217165`，因为只有 `20170701` 快照，对 2024 只能诊断；time-aligned known-event matches `0`，out-of-window overlaps `5167`；high_confidence strong `0`，gate_weak strong `0`，gate_weak insufficient `7182`；patternB path evidence entry `49852`；route_leak relation pending `364`；background_like evidence_supported `1189` | `s3d2_evidence_attachment_schema=完成_证据缺口明确`；S3-D2 成功挂载证据字段和 provenance，但没有完成 external verification；下一步应补 2024 RPKI/IRR/AS-rel cache 后复跑，或先做 S3-C3 route-leak triplet legality | 已完成 | `scripts/run_s3d2_external_evidence_attachment.py`，`project_docs/S3D2_EXTERNAL_EVIDENCE_ATTACHMENT.md`，`outputs/s3d2_external_evidence_attachment_v01/` |
| 45 | S3-D2B | S3-D2 暴露的外部证据缺口如何转成可执行的对齐清单 | 不重跑上游；不覆盖 S3-D/S3-D2；不在线批量查 RPKI/IRR；只做 evidence source inventory、prefix-origin targets、path relation targets、rerun manifest | fixed S2 full 完成：inspected incidents `217165`；valid prefix-origin lookup targets `40665`；path relation targets `168889`；local evidence sources `5`，其中 `as_relationship:stale=1`、`known_event_inventory:undated=4`；RPKI aligned cache available `false`；AS relationship aligned snapshot available `false`；known-event time-aligned overlaps `0`，out-of-window asset overlaps `6`；S3-D2 aligned rerun ready `false` | `s3d2b_evidence_alignment=完成_等待外部cache`；证据对齐从口头缺口变成 lookup target + cache schema + rerun manifest；下一步需取得 `2024-04-16` RPKI/ROA cache 与 2024-near AS-rel 后复跑 S3-D2，或先做 S3-C3 route-leak triplet legality | 已完成 | `scripts/run_s3d2b_evidence_alignment.py`，`project_docs/S3D2B_EVIDENCE_ALIGNMENT.md`，`outputs/s3d2b_evidence_alignment_v01/` |
| 46 | Phase R | 是否应继续按 detector-centric 线性实验推进，还是重定位为 verifier-centric 问题 | 不写新实验代码；不跑实验；只阅读 2023-2026 调研并更新长期文档；旧结果不删除、不改写 | 新增 Phase R 问题重定位、verifier redesign、learning layer positioning、paper problem statement；明确 public monitor output = candidate trigger，不是 final judge；high/needs/low 与 P1/P2/P3 降级为 legacy detector/priority signals；S3-A~S3-D2B 转为 Phase R historical groundwork | `phase_r_started_strategic_reframing`；旧 S3 线进入 strategic pause，不是失败；新主线为 adversarially robust multi-evidence BGP incident verification and triage；下一步是 R-1 verifier state machine 与 R-2 legality-first verifier | 已完成 | `project_docs/PHASE_R_PROBLEM_REFRAMING.md`，`project_docs/VERIFIER_REDESIGN_ROADMAP.md`，`project_docs/LEARNING_LAYER_POSITIONING.md`，`project_docs/PAPER_PROBLEM_STATEMENT.md` |
| 47 | Phase R-1 | verifier 如何把 legacy detector signals 转成 evidence states、verdict、confidence、provenance 和 abstain | 只做设计文档；不写实验代码；不跑 raw/events/baseline/candidate/score/gate/final；不修改主链产物；不把 high/needs/P1/P2/RPKI/P3 当 truth | 新增 verifier state machine 与 evidence/verdict registry；冻结 evidence states、verdict space、confidence caps、hard safety rules、implementation-ready schema draft、learning-layer boundary 与旧 S3 输出映射 | `r1_verifier_state_machine_design=完成`；R-1 明确 monitor output 只能作为 trigger，stale/unavailable/conflicting evidence 必须显式保留；learning layer 后置为 ranker/calibrator；下一步 R-2 才开始 legality-first verifier | 已完成 | `project_docs/R1_VERIFIER_STATE_MACHINE.md`，`project_docs/R1_EVIDENCE_TYPES_AND_VERDICTS.md`，`project_docs/VERIFIER_REDESIGN_ROADMAP.md` |
| 48 | Phase R-2A | 能否把 legacy incident 从 detector output 转成 legality/evidence-aware verifier verdict table | Scaffold + 50k smoke；不重跑 raw/events/baseline/candidate/score/gate/final；不覆盖旧 S3 输出；不训练学习模型；不做 poisoning benchmark；smoke 输出单独目录 | 新增 legality-first verifier scaffold 脚本；50k smoke 完成：processed `50000` incidents；verdict：`stale_evidence_only=43335`、`external_evidence_unavailable=3762`、`background_like_but_unconfirmed=1485`、`evidence_insufficient=1418`；`strongly_supported_suspicious=0`、`evidence_supported_suspicious=0`；RPKI `unavailable=50000`；path legality `stale_diagnostic=44632`、`not_applicable=5368`；stale rows `44807`、unavailable rows `50000` | `r2a_legality_first_verifier_scaffold_smoke=完成`；R-2A 成功生成 incident-level verifier table，但没有完成真实攻击判定；缺 aligned evidence cache 时显式输出 stale/unavailable/insufficient 是正确行为；下一步 R-2B refinement 或 evidence cache completion | 已完成 | `scripts/run_r2a_legality_first_verifier_scaffold.py`，`project_docs/R2A_LEGALITY_FIRST_VERIFIER_SCAFFOLD.md`，`outputs/r2a_legality_first_verifier_smoke_v01/` |
| 49 | Phase R-2B-0 | fixed S2 incidents 是否具备稳定外部证据 lookup key，本地 evidence cache 是否 ready | 只做 readiness audit；不下载外部大数据；不接入新 verifier 规则；不跑 full verifier；不覆盖旧 S3/R-2A 输出；不把 unavailable/stale 当 benign | 新增 evidence readiness audit 脚本；50k smoke 与 full incident audit 均完成；full processed `217165` incidents；prefix-origin complete `217162` (`0.999986`)；time-window complete `217165`；path-key complete `216922` (`0.998881`)；triplet-key complete `205067` (`0.944291`)；cache inventory：RPKI/VRP `missing`，IRR `missing`，ASPA `missing`，PeeringDB `missing`，known-event `present_unverified_schema`，AS-rel `ready_stale` only because `20170701.as-rel2.txt` exists | `r2b0_evidence_readiness_audit=完成`；incident schema 基本不阻塞外部 evidence lookup，当前主阻塞是 aligned external cache；下一步优先 P0b historical VRP/RPKI cache materialization for `2024-04-16`，再跑 R-2B/R-2C verifier smoke；learning layer 仍不可进入 | 已完成 | `scripts/run_r2b0_evidence_readiness_audit.py`，`project_docs/R2B0_EVIDENCE_READINESS_AUDIT.md`，`outputs/r2b0_evidence_readiness_audit_v01/` |
| 50 | Phase R-2B-P0b | 能否获取并标准化 `2024-04-16` 对齐 historical VRP/RPKI cache，并对 fixed S2 prefix-origin targets 做离线 origin validation lookup | 可以下载/构建 VRP/RPKI cache；不下载 IRR/PeeringDB/ASPA/data-plane；不改 R-1 verdict set；不运行 R-2C；不训练学习层；不覆盖旧 S3/R-2A/R-2B-0 输出；RPKI valid/invalid/unknown 都不是 truth | 新增 VRP materialization 脚本；source 为 RIPE NCC RPKI repository archive 的 5 个 TAL `2024/04/16/roas.csv.xz`；normalized VRP records `530187`；lookup eligible targets `217162`；RPKI status：`valid=122280`、`invalid_asn=397`、`invalid_length=283`、`unknown=94202`、`unavailable=0`；evidence states：`aligned_medium=122960`、`aligned_weak=94202`；safety violations `0`；未生成或修改 verifier verdict | `r2b_p0b_vrp_materialization=完成`；RPKI unavailable blocker 已解除，aligned origin authorization evidence 可进入 R-2B verifier smoke；但 RPKI 仍只验证 origin，不验证完整 AS path，不能单独推出 attack/benign；learning layer 仍不可进入 | 已完成 | `scripts/run_r2b_p0b_materialize_vrp_cache.py`，`project_docs/R2B_P0B_HISTORICAL_VRP_CACHE.md`，`data/evidence/rpki/vrp_2024-04-16.metadata.json`，`outputs/r2b_p0b_vrp_materialization_v01/` |
| 51 | Phase R-2B | aligned VRP evidence 接回 verifier 后，能否生成 component-aware incident verdict smoke，并维护 CCF-A target line | 不下载新外部证据；不接 IRR/ASPA/PeeringDB/data-plane；不做 R-2C path refinement；不训练学习层；不做 poisoning benchmark；不改 R-1 verdict set；不覆盖旧 S3/R-2A/R-2B-0/R-2B-P0b 输出；RPKI invalid/valid/unknown 都不是 truth；mixed incident 不强判 | 新增 VRP-aware verifier smoke 脚本与 CCF-A guardrail 文档；full processed `217165` incidents；dominant pair status：valid `122280`、unknown `94202`、invalid_asn `397`、invalid_length `283`、unavailable `3`；member status：valid `3048894`、unknown `2075964`、unavailable `348708`、invalid_length `3074`、invalid_asn `2663`；component purity：pure_dominant `114237`、insufficient `94666`、should_split `4689`、mostly_dominant `3023`、mixed_core_suspicious `479`、mixed_conflicting `71`; verdict：background_like `156971`、insufficient `55366`、abstain `4684`、conflict `86`、evidence_supported `55`、external_unavailable `3`、strong_supported `0`；hard safety violations `0` | `r2b_vrp_aware_verifier_smoke=完成`；第一批 aligned external evidence 已接入 verifier，RPKI unavailable 显著下降，但系统仍保持保守：只有 55 个 evidence-supported suspicious，0 个 strongly-supported；component/mixed incidents 被显式拆出用于 R-2C/L1；CCF-A target line 已冻结，下一步 R-2C path evidence branch + R-3 poisoning benchmark design + L1 component-aware learner design | 已完成 | `scripts/run_r2b_vrp_aware_verifier_smoke.py`，`project_docs/R2B_VRP_AWARE_VERIFIER_SMOKE.md`，`project_docs/CCFA_TARGET_LINE_AND_EXPERIMENT_GUARDRAILS.md`，`outputs/r2b_vrp_aware_verifier_smoke_v01/` |
| 52 | Phase R-2B-OPS | R-2B verifier 若走向真实部署，每日人工负担和实时 evidence cache 约束是什么 | 只做 operational burden + realtime evidence cache audit；不下载新证据；不修改 R-1/R-2B verdict；不训练 learning layer；不做 R-2C path evidence；outputs 只作本地审计产物 | 新增 ops audit 脚本；fixed S2 full audit processed `217165` incidents；日级线性外推：evidence_supported `220`、conflict `344`、abstain `18736`、insufficient `221464`、background_like `627884`、mixed_should_split `18756`、mixed_core `1916`；Top-K smoke：Top-50 supported density `1.0`，Top-100 `0.53`，Top-500 `0.11`；相对 legacy P1/P2 reduction proxy `0.9991/0.9982/0.9909`；no new evidence downloaded，verifier verdict modified `false` | `r2b_ops_realtime_burden_audit=完成`；R-2B-OPS 证明后续 claim 不能只说离线 verdict，还必须有 Top-K review policy、local evidence cache、freshness/fallback policy 和 SLO 口径；online path 不做 per-incident remote lookup；abstain/insufficient/background-like 不全量交给人工；下一步 R-2C，L1 只能做 design 不能训练 | 已完成 | `scripts/run_r2b_ops_realtime_burden_audit.py`，`project_docs/R2B_OPS_REALTIME_BURDEN_AUDIT.md`，`outputs/r2b_ops_realtime_burden_audit_v01/` |
| 53 | Phase R-2C-0 | fixed S2 incidents 是否具备 path/triplet/AS-pair lookup keys，path evidence cache 是否 ready | 只做 path evidence readiness audit；不下载新证据；不构建 AS-rel/ASPA/Roles cache；不修改 R-2B verdict；不训练 learning layer；不做 R-3；stale AS-rel 不能当 strong evidence；missing path evidence 不是 benign | 新增 path readiness audit 脚本；50k smoke 与 full audit 通过；full checked `217165` incidents；complete path-key `217162` (`0.999986`)；triplet-capable `217162` (`0.999986`)；AS-pair targets `216922`；triplet targets `205067`；full-path targets `217162`；cache inventory：2017 CAIDA AS-rel `ready_stale` only，2024-near AS-rel `missing`，ASPA `missing`，BGP Roles/OTC `missing`，PeeringDB `missing`，known-event path context `present_unverified_schema` | `r2c0_path_evidence_readiness_audit=完成`；path keys 基本 ready，当前阻塞是 aligned path evidence cache；不能直接进入 route-leak verifier；recommended next step 是 R-2C-P0b 2024-near AS relationship cache materialization，然后 ASPA/Roles feasibility 和 path-legality smoke；learning 仍不能训练 | 已完成 | `scripts/run_r2c0_path_evidence_readiness_audit.py`，`project_docs/R2C0_PATH_EVIDENCE_READINESS_AUDIT.md`，`outputs/r2c0_path_evidence_readiness_audit_v01/` |
| 54 | Phase R-2C-P0b | 能否直接构建 `2024-04-01` CAIDA AS Relationships cache，并用 R-2C-0 AS-pair targets 做 relation lookup smoke | 直接下载/构建 2024-near AS-rel cache；不再做 readiness audit；不做 route-leak verifier；不修改 R-2B verdict；不训练 learning；AS-rel violation 不是 confirmed route leak；AS-rel path合法不是 benign；large data 不提交 | 新增 AS-rel materialization 脚本；CAIDA source `https://data.caida.org/datasets/as-relationships/serial-2/20240401.as-rel2.txt.bz2` 下载成功；snapshot `2024-04-01`，run date `2024-04-16`，delta `15` days，future snapshot `false`；raw records `571330`，directed lookup records `1142660`，parse warnings `0`；AS-pair lookup：target rows `216922`，unique pairs `61997`，matched `56971`，unmatched `5026`，match rate `0.918932`，rel types `p2c_or_c2p_raw=33144`、`p2p=23827`、`unknown=5026` | `r2c_p0b_asrel_materialization=完成`；2024-near path evidence cache 已可用，下一步进入 R-2C-P1 path relation lookup smoke / path-legality smoke；仍不能直接声称 route leak，也不能进入 formal learning training | 已完成 | `scripts/run_r2c_p0b_materialize_asrel_cache.py`，`project_docs/R2C_P0B_2024_ASREL_CACHE.md`，`data/evidence/as_relationships/as_rel_2024-04-01.metadata.json`，`outputs/r2c_p0b_asrel_materialization_v01/` |
| 55 | Phase R-2C-P1 | 能否把 2024-near AS-rel relation lookup 接回 incident/component，生成 path evidence smoke | 不下载新证据；不做正式 route-leak verifier；不修改 R-2B verdict；不把 AS-rel matched 当 benign；不把 AS-rel unmatched 当 suspicious；possible valley-free 仅 diagnostic；不训练 learning；不做 NO_EXPORT/AS Hegemony/ASPA/Roles/OTC | 新增 path relation lookup smoke 脚本；py_compile、50k smoke、fixed S2 full 均通过；full expanded AS-pair rows `421989`，matched `399839`，unmatched `22150`，row-level match rate `0.947510`，rel types `p2c_or_c2p_raw=270770`、`p2p=129069`、`unknown=22150`；triplet rows `205067`，possible valley/peer-transit diagnostic rows `34192`；full-path rows `217162`，mean unknown pair rate `0.054964`; incident path states `aligned_medium=161992`、`diagnostic_only=34192`、`evidence_insufficient=18084`、`unavailable=2897`; route-leak-like diagnostic candidates `34555`; path-manipulation-like diagnostic candidates `44189`; combined classes include `origin_valid_path_suspicious=108583`、`origin_unknown_path_suspicious=86980`、`origin_and_path_supported=46` | `r2c_p1_path_relation_lookup_smoke=完成`；AS-rel 已从 cache readiness 进入 incident-level path evidence，但仍不生成 route-leak verdict，不修改 R-2B verdict，不把 AS-rel 当 truth；下一步进入 R-2C-P2 route-leak/path-legality verifier smoke，并补 ASPA/BGP Roles/OTC feasibility；learning 仍不能正式训练 | 已完成 | `scripts/run_r2c_p1_path_relation_lookup_smoke.py`，`project_docs/R2C_P1_PATH_RELATION_LOOKUP_SMOKE.md`，`outputs/r2c_p1_path_relation_lookup_smoke_v01/` |
| 56 | Phase R-2C-P2 | 能否把 R-2C-P1 path diagnostics 转成 R-1 verdict-compatible path-legality verifier smoke | 不下载新证据；不接 ASPA/BGP Roles/OTC、NO_EXPORT/communities、AS Hegemony；不修改 R-2B verdict；不生成 confirmed route leak；不把 AS-rel 当 truth；不训练 learning | 新增 path-legality verifier smoke 脚本；py_compile、50k smoke、fixed S2 full 均通过；full processed `217165` incidents；verdict smoke：`background_like_but_unconfirmed=127889`、`evidence_insufficient=78324`、`abstain=10822`、`evidence_conflict=86`、`evidence_supported_suspicious=44`；route-leak-like review candidates `34555`；path-manipulation-like review candidates `44189`；Top-K evidence-supported density：Top-50 `0.88`、Top-100 `0.44`、Top-500 `0.088`; `strongly_supported_suspicious=0`; hard safety violations `0` | `r2c_p2_path_legality_verifier_smoke=完成`；R-2C-P2 完成命名收紧并把 diagnostic cases 分入 review/insufficient/conflict/abstain/background queues；仍无 confirmed route leak、不改 R-2B verdict、不进入正式学习训练；下一步优先 R-2D-0 communities audit、L1 design、R-3 poisoning benchmark early design | 已完成 | `scripts/run_r2c_p2_path_legality_verifier_smoke.py`，`project_docs/R2C_P2_PATH_LEGALITY_VERIFIER_SMOKE.md`，`outputs/r2c_p2_path_legality_verifier_smoke_v01/` |
| 57 | Phase R-LOCK-1 | 如何防止系统被描述成旧 7 层 + 多证据 + verifier + learning 的模块堆砌 | 只做架构收敛文档和最小性 / 消融计划；不跑新实验；不下载新证据；不修改 verifier outputs；不训练 learning；不做 NO_EXPORT/AS Hegemony/poisoning benchmark | 新增 minimal architecture lock 与 simplified output schema；最终架构压缩为 Stage 1 Monitor-triggered Incident Construction、Stage 2 Evidence-constrained Verification、Stage 3 Component-aware Learning Triage，Top-K 为 human-facing queue；旧 7 层重新解释为 Stage 1；定义 module non-overlap、failure modes、A0-A9 ablation plan、reviewer attack/defense、incident card schema | `r_lock1_architecture_minimality=完成`；系统贡献口径从模块列表收敛为三阶段最小架构；后续每个模块都必须能对应 failure mode 和 ablation，learning 明确位于 verifier 与 Top-K 之间且不能覆盖 hard rules | 已完成 | `project_docs/ARCHITECTURE_MINIMALITY_AND_ABLATION_PLAN.md`，`project_docs/SYSTEM_OUTPUT_SCHEMA_SIMPLIFIED.md` |
| 58 | Phase R-2D-0 | 当前 fixed S2 raw/event/incident/verifier pipeline 是否保留 BGP communities 字段，能否解析 NO_EXPORT / NO_ADVERTISE 并 join 到 incident | 只做字段可用性审计；固定 run `s2a_expanded_v01_pilot_6h_april16`，run date `2024-04-16`；扫描 fixed run root、sibling collector raw runs、R-2B/R-2C verifier tables；不下载新证据、不修改 verdict、不训练 learning、不实现 stealth verifier、不检测 NO_EXPORT attack | 新增 communities audit 脚本；py_compile、50k smoke、fixed S2 full audit 均通过；full scanned files `893`，raw files with `communities` `864`；raw rows `39039005`，non-empty communities `35657850`，parse success `27843441`；well-known rows：`NO_EXPORT=223410`、`NO_ADVERTISE=1240`、`NO_EXPORT_SUBCONFED=0`、`NOPEER=5634`；communities 只在 `raw_updates` 层保留，`event_units`/`incident_membership`/`incident_tickets`/verifier outputs 均未保留；incident join ready `false` | `r2d0_communities_field_availability_audit=完成_pipeline_repair_needed`；raw communities evidence channel 存在且可解析，但当前不能进入 R-2D-1 stealth evidence branch；第一修复目标是 `scripts/build_event_units.py`，随后检查 incident membership/card 传播；NO_EXPORT present 不是 confirmed attack，NO_EXPORT absent 不是 safe，low visibility 不是 NO_EXPORT | 已完成 | `scripts/run_r2d0_communities_field_availability_audit.py`，`project_docs/R2D0_COMMUNITIES_FIELD_AVAILABILITY_AUDIT.md`，`outputs/r2d0_communities_field_availability_audit_v01/` |

## 当前主线阶段判断

- 历史阶段：已经完成主链因果、可见度、案例、已知事件、双轨制评估的核心闭环。
- 当前主任务：Phase R 已启动。历史阶段已经收口，modern/S2/S3 资产保留为 verifier-centric historical groundwork：S3-A/S3-A2/S3-B 证明 event -> incident -> calibrated priority -> noise attribution 框架可行；S3-C1/S3-C1b/S3-C2 证明 plausibility 更适合作为 gate/verifier evidence；S3-D/S3-D2/S3-D2B 建立 verification queue、evidence attachment 和 evidence alignment。旧实验线进入 strategic pause，不宣称失败，但不再以 raw detector score 或 event-level high 数量作为主问题。
- 当前默认下一步：Phase R-LOCK-1 已完成，系统已收敛为 Stage 1 Monitor-triggered Incident Construction、Stage 2 Evidence-constrained Verification、Stage 3 Component-aware Learning Triage + Top-K Review Queue。aligned VRP/RPKI evidence、2024-near CAIDA AS-rel evidence、incident-level path relation evidence、path-legality verifier smoke、operational burden / realtime cache guardrail、以及 R-2D-0 communities field availability audit 都是该三阶段架构的 groundwork；下一步优先做 community-retention pipeline repair，将 raw `communities` 传递到 `event_units` 和 incident membership/card 后复跑 R-2D-0，再决定是否进入 R-2D-1 community-aware stealth evidence branch，同时推进 R-3 poisoning benchmark design 和 L1 component-aware semantic learner design。S3-D/S3-D2 queue 和 status candidate 不是真实标签，S3-D2B lookup targets、R-2B-0 readiness states、R-2B-P0b RPKI lookup statuses、R-2B VRP-aware verdicts、R-2B-OPS workload projection、R-2C-0 readiness states、R-2C-P0b AS-rel lookup states、R-2C-P1 path diagnostic classes、R-2C-P2 path-legality smoke verdicts 与 R-2D-0 community field hits 也不是最终真假标签；学习层位于 verifier 与 Top-K 之间，是 component-aware incident-level ranker / evidence calibrator，不能覆盖 verifier hard rules；24h expanded 应作为 verifier/incident 口径实验，不要回到纯 event-level 评估。

## 后续维护规则

- 新实验结束后，直接在本表最后新增一行。
- 若已有实验只是补充结论或修正状态，不新开第二张表，直接更新对应行。
- `状态` 统一只用：`已完成`、`进行中`、`暂停/待续`。
- `主要产物 / 代码` 只填正式可复用路径，不填临时调试目录。
